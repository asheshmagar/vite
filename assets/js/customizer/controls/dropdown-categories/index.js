import { defineControls } from '../../utils';
import DropdownCategories from './dropdown-categories';

export default () => {
	defineControls( 'customind-dropdown-categories', DropdownCategories );
};
